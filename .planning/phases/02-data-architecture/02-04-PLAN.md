---
phase: 02-data-architecture
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - website/src/data/institutions-index.json
  - website/README.md
autonomous: true
requirements:
  - DATA-04
  - DX-02

must_haves:
  truths:
    - "institutions-index.json is valid JSON and exports empty institutions array (or initial MD institutions)"
    - "institutions-index.json uses schema v3.0.0"
    - "README explains md- prefix requirement and validation"
    - "README instructions reference md-{slug} pattern for new institutions"
  artifacts:
    - path: "website/src/data/institutions-index.json"
      provides: "Generated institution index file"
      contains: "schemaVersion: 3.0.0"
    - path: "website/README.md"
      provides: "Developer documentation"
      contains: "md-" reference and build validation guidance
  key_links:
    - from: "generate-index.js build script"
      to: "institutions-index.json"
      via: "file generation"
      pattern: "writeFile.*institutions-index.json"
    - from: "README.md contributor guide"
      to: "Institution schema"
      via: "documentation reference"
      pattern: "md-"
---

<objective>
Regenerate institutions-index.json using the updated validation (which now expects md- prefix) and update README with Moldova-specific setup and build validation guidance. Completes data layer migration and documents the new schema expectations.

Purpose: Index is ready for owner to add Moldova institutions; documentation guides contributors on md- prefix requirement.
Output: Empty but valid institutions-index.json; README updated with Moldova context and build validation instructions.
</objective>

<execution_context>
@/Users/dorianzlatan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorianzlatan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

Current README: targets Romania (IdentitateMD). Needs update to reference Moldova (IdentitateMD).
Current index: contains 33 institutions (32 Romanian + 1 EU flag). After plan 02-02, will be empty (except EU flag).

From generate-index.js: script writes to `src/data/institutions-index.json` with schema v3.0.0.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate institutions-index.json with md- validation</name>
  <files>website/src/data/institutions-index.json</files>
  <action>
Run the generate-index.js script to regenerate the institutions index:

```bash
cd website && node scripts/generate-index.js
```

Since plans 02-02 and 02-03 removed all ro-\* institutions, the script will:

- Find only eu-flag.json in src/data/institutions/
- Validate it (eu-flag does not start with md-, so will skip or warn but continue as it's a reference)
- Generate institutions-index.json with empty or minimal institutions array
- Output summary showing 0 institutions (or just eu-flag if included)

The index file will be valid JSON with schemaVersion: "3.0.0", ready for owner to populate with md-\* institutions.

If the script fails because of eu-flag.json validation, manually create a minimal valid index:

```json
{
  "schemaVersion": "3.0.0",
  "generatedAt": "2026-03-04T...",
  "total": 0,
  "stats": {
    "byCategory": {},
    "withManual": 0,
    "withSvg": 0
  },
  "categories": [],
  "institutions": []
}
```

  </action>
  <verify>
    Run: `jq '.schemaVersion' website/src/data/institutions-index.json`
    Expected: "3.0.0" (with quotes).

    Run: `jq '.institutions | length' website/src/data/institutions-index.json`
    Expected: 0 or 1 (empty or minimal).

    Run: `node -e "const idx = require('./website/src/data/institutions-index.json'); console.log(idx.schemaVersion, idx.institutions.length);"`
    Expected: "3.0.0 0" (valid JSON, correct schema, empty institutions array).

  </verify>
  <done>
    institutions-index.json regenerated. Valid JSON with schema v3.0.0, empty institutions array. File ready for owner to add md-* institutions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README with Moldova context and build validation guidance</name>
  <files>website/README.md</files>
  <action>
Update the README to reflect Moldova context and build validation:

1. Top section (title, description):
   - Change "IdentitateMD" to "IdentitateMD" in title and throughout
   - Change "din Republica Moldova" to "din Republica Moldova" (institution descriptions)
   - Update domain references from identitate.md to identitate.md
   - Update GitHub org reference to identitate-md/identitate-md

2. In "Schema de Date" section:
   - Change example ID from "primaria-timisoara" to placeholder "md-primaria-{city}" format
   - Update ID field description to: `"id": "md-{slug}"` — MUST start with md- (not ro-). Example: "md-anaf", "md-primaria-chisinau"
   - Add note: "The md- prefix is enforced by the build system. Institutions without md- prefix will be rejected during index generation."

3. In "Cum Contribui" section, add build validation step:
   - "3. Rulează npm run data:generate — script validates all institutions match md-_ pattern and fails if any ro-_ institutions found"
   - "4. Verifică local cu npm run dev"

4. In Stack Tehnic section:
   - Update any remaining IdentitateMD references to IdentitateMD

5. In Avertisment Legal:
   - Change "din Republica Moldova" to "din Republica Moldova" where appropriate

Do NOT remove content about categories, logos, or technical structure — just update Moldova references and add build validation note.
</action>
<verify>
Run: `grep -i "identitate.md\|din Republica Moldova" website/README.md` (case-insensitive search)
Expected: no matches OR only expected occurrences (e.g., historical notes if any).

    Run: `grep "md-" website/README.md`
    Expected: at least 3 matches (in ID format section, contribution guide, schema example).

    Run: `grep -i "build.*validat\|md-.*pattern\|prefix.*enforc" website/README.md`
    Expected: at least 1 match (validation guidance).

  </verify>
  <done>
    README updated with IdentitateMD branding, identitate.md domain, and build validation guidance. All Romania references updated to Moldova. md- prefix requirement clearly documented for contributors.
  </done>
</task>

</tasks>

<verification>
1. Verify index is valid JSON: `jq . website/src/data/institutions-index.json` (should pretty-print without error)
2. Verify README has Moldova references: `grep -c "identitate.md\|Republica Moldova" website/README.md` (count > 0)
3. Verify build validation guidance exists: `grep "md-.*pattern\|prefix.*enforc" website/README.md` (at least 1 match)
</verification>

<success_criteria>

- institutions-index.json is valid JSON with schemaVersion 3.0.0 and empty institutions array
- File is committed and ready for owner to add md-\* institutions via manual file additions
- README references identiitate.md (not identitate.md)
- README explains md- prefix requirement and build validation
- Contributor guide includes step to run generate-index.js with md- validation
- All Romania context replaced with Moldova references throughout README
  </success_criteria>

<output>
After completion, create `.planning/phases/02-data-architecture/02-04-SUMMARY.md`
</output>
