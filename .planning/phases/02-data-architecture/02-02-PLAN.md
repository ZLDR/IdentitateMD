---
phase: 02-data-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/data/institutions/
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "All ro-*.json institution files removed from src/data/institutions/"
    - "Only eu-flag.json and institutions-index.json remain (index is empty)"
    - "Directory is ready to accept md-* institutions"
  artifacts:
    - path: "website/src/data/institutions/"
      provides: "Institution data directory"
      contains: "Only .json files for Moldova institutions"
  key_links:
    - from: "src/data/institutions/ directory"
      to: "generate-index.js input"
      via: "file enumeration"
      pattern: "readdir.*INSTITUTIONS_DIR"
---

<objective>
Remove all 32 existing Romanian institution JSON files from the data directory. Clears Romanian data, leaving directory ready for Moldova institutions.

Purpose: Data migration — remove legacy Romania institutions; prepare for new Moldovan data.
Output: src/data/institutions/ directory contains only eu-flag.json (EU reference) and generated institutions-index.json.
</objective>

<execution_context>
@/Users/dorianzlatan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorianzlatan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Current state: 33 institution JSON files in website/src/data/institutions/
- 32 ro-*.json files (Romanian institutions)
- 1 eu-flag.json (EU flag, used as reference — KEEP)
- 1 institutions-index.json (generated, will be recreated empty)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Romanian institution JSON files</name>
  <files>website/src/data/institutions/</files>
  <action>
Delete all Romanian (ro-*) institution JSON files from website/src/data/institutions/:

Use: `cd website && find src/data/institutions -name "ro-*.json" -delete`

This removes all 32 Romanian institution files. The eu-flag.json will remain (it is a reference institution, not Romania-specific). The institutions-index.json will be regenerated in plan 02-04.

Verify deletion with: `ls src/data/institutions/` — should show only eu-flag.json and institutions-index.json.
  </action>
  <verify>
    Run: `ls website/src/data/institutions/ | grep "^ro-"` or `find website/src/data/institutions -name "ro-*.json"`
    Expected: no output (zero matches).

    Run: `ls website/src/data/institutions/`
    Expected: shows eu-flag.json and institutions-index.json only (2 files).
  </verify>
  <done>
    All 32 ro-*.json files deleted. Only eu-flag.json and institutions-index.json remain in directory.
  </done>
</task>

</tasks>

<verification>
Verify no Romanian institutions remain: `find website/src/data/institutions -name "ro-*.json" -type f` returns no output.
</verification>

<success_criteria>
- All ro-*.json files deleted from website/src/data/institutions/
- eu-flag.json still exists (reference institution)
- institutions-index.json still exists (will be regenerated empty in wave 2)
- Directory ready to accept md-* institution files
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-architecture/02-02-SUMMARY.md`
</output>
