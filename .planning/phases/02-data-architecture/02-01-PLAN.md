---
phase: 02-data-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/types/institution.ts
  - website/scripts/generate-index.js
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "Institution type guards expect md- prefix, not ro-"
    - "generate-index.js validates institutions start with md-"
    - "generate-index.js rejects ro- prefixed institutions with error"
  artifacts:
    - path: "website/src/types/institution.ts"
      provides: "Type guards for Institution schema"
      contains: "obj.id.startsWith('md-')"
    - path: "website/scripts/generate-index.js"
      provides: "Validation logic for institution index generation"
      contains: "startsWith('md-')"
  key_links:
    - from: "generate-index.js validation"
      to: "Institution type guard"
      via: "both check md- prefix"
      pattern: "startsWith\\('md-'\\)"
---

<objective>
Update all validation scripts and type guards to enforce `md-` prefix instead of `ro-`. Ensures build system rejects Romanian institution IDs and accepts only Moldovan ones.

Purpose: Data layer schema validation must fail if any `ro-` prefixed institutions exist.
Output: Type guards and build validation expect `md-{slug}` IDs.
</objective>

<execution_context>
@/Users/dorianzlatan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorianzlatan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

From website/src/types/institution.ts (current — has ro- checks):
- Line 162: `id: string;` comment says `"ro-{slug}" format`
- Line 201: `obj.id.startsWith('ro-') &&`
- Line 222: `obj.id.startsWith('ro-') &&`

From website/scripts/generate-index.js (current — has ro- checks):
- Line 65: `if (!data.id.startsWith('ro-') || !data.meta?.keywords || !data.assets?.main)`
- Line 66: `console.error(...nu respectă schema v3.0...)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Institution type guards for md- prefix</name>
  <files>website/src/types/institution.ts</files>
  <action>
Update type guard functions to enforce `md-` prefix for Moldovan institutions:

Line 162 comment: change from `"ro-{slug}" format` to `"md-{slug}" format`

In `isInstitution()` function (line 201): change `obj.id.startsWith('ro-')` to `obj.id.startsWith('md-')`

In `hasCurrentSchema()` function (line 222): change `obj.id.startsWith('ro-')` to `obj.id.startsWith('md-')`

Do NOT change Location interface (country_code still "RO" is acceptable for now — it refers to geographic location data structure inherited from fork, focus only on ID prefix validation).

Verify the file compiles by running: `cd website && npx tsc --noEmit`
  </action>
  <verify>
    Run: `grep "startsWith('ro-'" website/src/types/institution.ts`
    Expected: no output (zero matches for ro- checks).

    Run: `grep "startsWith('md-'" website/src/types/institution.ts`
    Expected: 2 matches (lines in isInstitution and hasCurrentSchema).
  </verify>
  <done>
    Both type guard functions check for md- prefix. No ro- prefix checks remain. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update generate-index.js validation to reject ro- institutions</name>
  <files>website/scripts/generate-index.js</files>
  <action>
Update validation logic to reject Romanian institutions and accept only Moldovan:

Line 65-66: Change validation condition from:
```js
if (!data.id.startsWith('ro-') || !data.meta?.keywords || !data.assets?.main) {
  console.error(`  ⚠️  ${file}: nu respectă schema v3.0 (lipsește ro- prefix...`);
```

To:
```js
if (!data.id.startsWith('md-') || !data.meta?.keywords || !data.assets?.main) {
  console.error(`  ⚠️  ${file}: nu respectă schema v3.0 (lipsește md- prefix...`);
```

Also update file header comment (line 1-10) from `IdentitateRO` to `IdentitateMD`.

The script will now reject any institution not starting with md-, including all existing ro-* institutions.
  </action>
  <verify>
    Run: `grep "startsWith('ro-'" website/scripts/generate-index.js`
    Expected: no output (zero matches).

    Run: `grep "startsWith('md-'" website/scripts/generate-index.js`
    Expected: 1 match (in validation condition).
  </verify>
  <done>
    generate-index.js validates institutions expect md- prefix. Script rejects ro- prefixed files. No ro- checks remain.
  </done>
</task>

</tasks>

<verification>
Full validation sweep: `grep -r "startsWith('ro-'" website/src/types/institution.ts website/scripts/generate-index.js` returns no output.
</verification>

<success_criteria>
- institution.ts: both isInstitution() and hasCurrentSchema() check for md- prefix
- generate-index.js: validation requires md- prefix and rejects ro-
- Build will fail on existing ro-* institution files (expected; they are removed in 02-02)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-architecture/02-01-SUMMARY.md`
</output>
