---
phase: 02-data-architecture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - website/public/logos/
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "All ro-* logo directories removed from public/logos/"
    - "Only md-* and eu-flag logo directories remain (or will exist)"
    - "Directory structure ready for Moldovan institution logos"
  artifacts:
    - path: "website/public/logos/"
      provides: "Logo asset directory"
      contains: "Only md-{slug} pattern directories"
  key_links:
    - from: "generate-index.js"
      to: "public/logos/ directory"
      via: "asset path verification"
      pattern: "public/logos/md-"
---

<objective>
Remove all Romanian logo directories (ro-*) from public/logos/. Cleans logo assets, leaving directory ready for Moldovan institution logos.

Purpose: Asset cleanup — remove Romanian logos; prepare for new Moldovan branding assets.
Output: public/logos/ contains only eu-flag and md-* directories (currently none, will be added by owner).
</objective>

<execution_context>
@/Users/dorianzlatan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorianzlatan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Current state: website/public/logos/ contains:
- ~32 ro-* directories with SVG/PNG logo files
- 1 eu-flag directory (EU reference flag — KEEP)

Each ro-* directory has structure like:
  ro-{name}/
    ├── symbol/
    │   ├── color.svg
    ├── horizontal/
    │   ├── color.svg, white.svg, black.svg
    ├── vertical/
    └── metadata.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Romanian logo directories</name>
  <files>website/public/logos/</files>
  <action>
Delete all Romanian (ro-*) logo directories from website/public/logos/:

Use: `cd website && find public/logos -maxdepth 1 -type d -name "ro-*" -exec rm -rf {} +`

This removes all ro-* directories and their contents (SVG/PNG files). The eu-flag directory will remain (it is a reference institution).

Verify deletion with: `ls public/logos/` — should show only eu-flag directory (and any md-* directories if they exist, but currently none).
  </action>
  <verify>
    Run: `ls website/public/logos/ | grep "^ro-"` or `find website/public/logos -maxdepth 1 -type d -name "ro-*"`
    Expected: no output (zero directories matching ro-*).

    Run: `ls -d website/public/logos/*/`
    Expected: shows only eu-flag/ and any md-* directories (currently just eu-flag/).
  </verify>
  <done>
    All ro-* logo directories deleted. Only eu-flag and any md-* directories remain in public/logos/.
  </done>
</task>

</tasks>

<verification>
Verify no Romanian logo directories remain: `find website/public/logos -maxdepth 1 -type d -name "ro-*"` returns no output.
</verification>

<success_criteria>
- All ro-* directories deleted from website/public/logos/
- eu-flag directory still exists (reference institution)
- Directory structure ready for md-* logo directories
- Owner can now add institutions with md-{slug} pattern logos
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-architecture/02-03-SUMMARY.md`
</output>
