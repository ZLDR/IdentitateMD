---
phase: 01-branding-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/astro.config.mjs
  - vercel.json
  - website/package.json
  - packages/logos/package.json
  - website/src/lib/cdn-helpers.ts
autonomous: true
requirements:
  - BRAND-01
  - BRAND-03
  - BRAND-04

must_haves:
  truths:
    - "astro.config.mjs site field is https://identitate.md"
    - "vercel.json CDN rewrites reference @identitate-md/logos (not @identitate-ro)"
    - "website/package.json name and description reference Moldova, not Romania"
    - "packages/logos/package.json name is @identitate-md/logos"
    - "cdn-helpers.ts CDN_PATTERNS use @identitate-md/logos package name"
  artifacts:
    - path: "website/astro.config.mjs"
      provides: "Correct site URL for sitemap and canonical links"
      contains: "identitate.md"
    - path: "vercel.json"
      provides: "CDN rewrites for logo delivery"
      contains: "@identitate-md/logos"
    - path: "website/src/lib/cdn-helpers.ts"
      provides: "CDN URL generation functions"
      contains: "@identitate-md/logos"
  key_links:
    - from: "vercel.json rewrites"
      to: "cdn.jsdelivr.net/npm/@identitate-md/logos"
      via: "source: /logos/:path*"
      pattern: "@identitate-md/logos"
    - from: "cdn-helpers.ts CDN_PATTERNS"
      to: "cdn.jsdelivr.net/npm/@identitate-md/logos"
      via: "jsdelivr/unpkg pattern strings"
      pattern: "@identitate-md/logos"
---

<objective>
Update all configuration files and package references from Romania (`@identitate-ro`) to Moldova (`@identitate-md`). Sets the site URL to identitate.md and updates CDN delivery to reference the Moldova logos package.

Purpose: Foundation for correct canonical URLs in sitemap, OG tags, and CDN asset delivery.
Output: astro.config.mjs, vercel.json, package.json files, cdn-helpers.ts all reference Moldova.
</objective>

<execution_context>
@/Users/dorianzlatan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorianzlatan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site URL and package names in config files</name>
  <files>website/astro.config.mjs, vercel.json, website/package.json, packages/logos/package.json</files>
  <action>
    In `website/astro.config.mjs`: change `site: 'https://identitate.eu'` to `site: 'https://identitate.md'`.

    In `vercel.json`: change both CDN rewrite destination URLs from `@identitate-ro/logos@1.3.4` to `@identitate-md/logos@1.3.4`. Specifically update:
    - `/loader.js` destination: `https://cdn.jsdelivr.net/npm/@identitate-md/logos@1.3.4/identity-loader.js`
    - `/logos/:path*` destination: `https://cdn.jsdelivr.net/npm/@identitate-md/logos@1.3.4/logos/:path*`

    In `website/package.json`: change `"name": "identitate-ro"` to `"name": "identitate-md"`, and `"description"` from `...din România` to `...din Republica Moldova`. Change `"author"` from `IdentitateRO Contributors` to `IdentitateMD Contributors`.

    In `packages/logos/package.json`: change `"name": "@identitate-ro/logos"` to `"@identitate-md/logos"`. Update `"description"` to reference Moldova. Update `"author"` to `IdentitateMD Contributors`. Update `"homepage"` to `https://identitate.md`. Update `"repository"` URL to the Moldova repo (use `https://github.com/identitate-md/identitate-md` as placeholder — owner will update). Remove `"romania"` from keywords, add `"moldova"`. Update `"keywords"` description to reflect Moldova.
  </action>
  <verify>
    Run: `grep -r "identitate\.eu\|@identitate-ro" website/astro.config.mjs vercel.json website/package.json packages/logos/package.json`
    Expected: no output (zero matches).
  </verify>
  <done>
    All four config files reference identitate.md and @identitate-md/logos. Zero occurrences of identitate.eu or @identitate-ro in these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CDN helper package references</name>
  <files>website/src/lib/cdn-helpers.ts</files>
  <action>
    In `website/src/lib/cdn-helpers.ts`:

    Update the file header comment from `CDN Helpers for IdentitateRO` to `CDN Helpers for IdentitateMD`.

    Update `CDN_PATTERNS` — change both template strings from `@identitate-ro/logos` to `@identitate-md/logos`:
    ```ts
    jsdelivr: (version: string, path: string) =>
      `https://cdn.jsdelivr.net/npm/@identitate-md/logos@${version}${path}`,
    unpkg: (version: string, path: string) =>
      `https://unpkg.com/@identitate-md/logos@${version}${path}`,
    ```

    The `readLogosVersion()` function reads `packages/logos/package.json` which is now named `@identitate-md/logos` — no path change needed, it reads by filesystem path not package name. Leave `readLogosVersion` function intact.

    Update JSDoc comments in the file that mention IdentitateRO to IdentitateMD where they appear.
  </action>
  <verify>
    Run: `grep "@identitate-ro" website/src/lib/cdn-helpers.ts`
    Expected: no output.
  </verify>
  <done>
    cdn-helpers.ts CDN_PATTERNS generate URLs with @identitate-md/logos. No @identitate-ro references remain.
  </done>
</task>

</tasks>

<verification>
`grep -r "@identitate-ro\|identitate\.eu" website/astro.config.mjs vercel.json website/package.json packages/logos/package.json website/src/lib/cdn-helpers.ts` returns no matches.
</verification>

<success_criteria>
- astro.config.mjs: `site` is `https://identitate.md`
- vercel.json: both CDN rewrites reference `@identitate-md/logos@1.3.4`
- website/package.json: name is `identitate-md`, description references Moldova
- packages/logos/package.json: name is `@identitate-md/logos`
- cdn-helpers.ts: CDN_PATTERNS use `@identitate-md/logos`
</success_criteria>

<output>
After completion, create `.planning/phases/01-branding-typography/01-01-SUMMARY.md`
</output>
